= javascript_include_tag "https://www.gstatic.com/charts/loader.js" unless Rails.env.test? #, async: true - In order to load it asynchronously, we need to load the pie charts after loader.js is loaded (use callback?). Otherwise we get ReferenceError: google is not defined
.text-center
  Filter by occupation/university by clicking on the pies:
#stats
  .row
    .col-md-6
      #occupations_pie{style: "width: 500px; height: 400px;"}
    .col-md-6
      #schools_pie{style: "width: 500px; height: 400px;"}
  = render partial: "common/occupations_pie_script"
  = render partial: "common/schools_pie_script"
